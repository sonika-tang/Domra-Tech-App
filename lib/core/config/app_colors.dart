import 'package:flutter/material.dart';

class AppColors {
  //primary Brand
  static const primary = Color(0xFF3F51B5); // header, secondary button, icon
  static const primaryHover = Color(0xFF303F9F); //hover state
  static const primaryBackground = Color(0xFFC5CAE9); //light background
  static const primarySection = Color(0xFFE8EAF6); //primary sections color

  //Secondary color
  static const secondary = Color(0xFFE4A54D); //main button, hightlights
  static const secondaryHover = Color(0xFFD9902D); //hover state
  static const secondaryBackground = Color(0xFFFFF4E1); //soft acient background

  // Text and background
  static const background = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF424242);
  static const gray = Color(0xFF9E9E9E); //Placeholders, low emphasis
  static const lightGray = Color(0xFFE0E0E0); //Borders, dividers

  // Static color
  static const success = Color(0xFF4CAF50); //Confirmed action, completed translation
  static const warining = Color(0xFFFFC107); //warining
  static const error = Color(0xFFF44336); //input validation, transaction reject
  static const information = Color(0xFF2196F3); //notification 
}
